/**
 *
 * @param {array} people
 * @param {number} limit
 */
export const solution = (people, limit) => {
  let answer = 0;
  people.sort((a, b) => a - b);
  for(let i = 0; i < people.length; i++) {
    if () {

    } else {

    }
  }
};
