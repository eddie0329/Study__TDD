import { solution } from "./타겟 넘버";

describe("타겟 넘버 test", () => {
  it("solution test1", () => {
    expect(solution([1, 1, 1], 2)).toBe(1);
  });
  // it("solution test2", () => {
  //   expect(solution([1, 1, 1, 1, 1], 3)).toBe(5);
  // });
});
